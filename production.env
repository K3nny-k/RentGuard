# Production Environment Configuration
# Copy this to .env and update with your secure values

# Database Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_minimum_32_characters

# MinIO Configuration
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key_minimum_8_chars
MINIO_BUCKET_NAME=rentguard-images

# Application URLs (Update with your server IP)
NEXT_PUBLIC_API_URL=http://20.55.40.193/api
NEXT_PUBLIC_UPLOAD_URL=http://20.55.40.193/api/upload

# Server Configuration
API_PORT=3001
API_HOST=0.0.0.0

# Security Notes:
# 1. Generate strong passwords for all services
# 2. Use a secure JWT secret (minimum 32 characters)
# 3. Replace default MinIO credentials
# 4. Consider using environment-specific secrets management 